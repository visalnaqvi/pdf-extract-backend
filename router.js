// import express from 'express';
// import multer from 'multer';
// import Controller from './controller.js';

// const router = express.Router();
// const controller = new Controller();

// // Create a Multer instance with your storage configuration
// const storage = multer.memoryStorage(); // Example: You can customize this storage configuration as needed.
// const upload = multer({ storage });

// // Apply the Multer middleware to the specific route
// router.post('/upload', upload.single('pdfFile'), controller.upload);

// export default router;
